{
  "homepage": "https://rayhanga.github.io",
  "name": "my-portfolio",
  "version": "0.3.4",
  "private": true,
  "dependencies": {
    "phaser-ce": "^2.10.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-responsive": "^4.0.4",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d build",
    "postdeploy": "git push origin develop && git push --tags",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "bump_major": "npm version major -m '%s' ",
    "bump_minor": "npm version minor -m '%s' ",
    "bump_patch": "npm version patch -m '%s' "
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
